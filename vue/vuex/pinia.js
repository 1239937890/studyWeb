//defineStore()

//第一个参数id为容器的名字，名字必须唯一，不能重复

//第二个参数setup为一个配置对象，里面是对容器仓库的配置说明，说明是以对象的形式

//state属性
//用来存储全局的状态的，里面定义的是为SPA里全局的状态

//getters属性
// 用来监视或计算状态的变化，有缓存的功能

//actions属性
//对state里数据变化的业务逻辑，就是用来修改state全局状态数据的

//第三个参数options